sudo: required
dist: trusty
language: node_js
node_js:
  - '6.11'

services:
  - docker

env:
  global:
     BLUEMIX_TARGET_URL="https://api.eu-de.bluemix.net"

deploy:
  provider: script
  skip_cleanup: true
  script: "bash $TRAVIS_BUILD_DIR/deploy.sh"
  on:
    branch: master

