version: '2.1'
services:
  server:
    build: server
    ports:
      - 3000:3000
  client:
    build: saevis
    ports:
      - 80:80
      - 443:443
    environment:
      - SERVER_URL=https:\/\/localhost

